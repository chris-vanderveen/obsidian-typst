# Cases


## cases / display

```typst
cases(#CURSOR "if" #CURSOR, #CURSOR "else")
```

## casesn (script)

`e.g. 3`

```js
const n = Number(input);

return `cases(\n${(`  #CURSOR "if" #CURSOR,\n`).repeat(n-1)}  #CURSOR "else"\n)`
```
